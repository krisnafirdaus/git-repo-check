// Import jest-dom untuk memperluas expect dengan matchers DOM
import '@testing-library/jest-dom';

// Menyiapkan lingkungan pengujian global
global.ResizeObserver = jest.fn().mockImplementation(() => ({
  observe: jest.fn(),
  unobserve: jest.fn(),
  disconnect: jest.fn(),
}));

// Menyiapkan mocks untuk API browser yang mungkin tidak tersedia di lingkungan pengujian
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: jest.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: jest.fn(),
    removeListener: jest.fn(),
    addEventListener: jest.fn(),
    removeEventListener: jest.fn(),
    dispatchEvent: jest.fn(),
  })),
});
